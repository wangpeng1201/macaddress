<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foxconn.sw.macaddress.dao.LivestockDao">

    <resultMap type="com.foxconn.sw.macaddress.entity.Livestock" id="LivestockMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="startMacAddress" column="start_mac_address" jdbcType="VARCHAR"/>
        <result property="endMacAddress" column="end_mac_address" jdbcType="VARCHAR"/>
        <result property="currentStock" column="current_stock" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="LivestockMap">
        select
          id, start_mac_address, end_mac_address, current_stock
        from bill.livestock
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="LivestockMap">
        select
          id, start_mac_address, end_mac_address, current_stock
        from bill.livestock
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="LivestockMap">
        select
        id, start_mac_address, end_mac_address, current_stock
        from bill.livestock
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="startMacAddress != null and startMacAddress != ''">
                and start_mac_address = #{startMacAddress}
            </if>
            <if test="endMacAddress != null and endMacAddress != ''">
                and end_mac_address = #{endMacAddress}
            </if>
            <if test="currentStock != null">
                and current_stock = #{currentStock}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into bill.livestock(start_mac_address, end_mac_address, current_stock)
        values (#{startMacAddress}, #{endMacAddress}, #{currentStock})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bill.livestock(start_mac_address, end_mac_address, current_stock)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.startMacAddress}, #{entity.endMacAddress}, #{entity.currentStock})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bill.livestock(start_mac_address, end_mac_address, current_stock)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.startMacAddress}, #{entity.endMacAddress}, #{entity.currentStock})
        </foreach>
        on duplicate key update
        start_mac_address = values(start_mac_address) , end_mac_address = values(end_mac_address) , current_stock =
        values(current_stock)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update bill.livestock
        <set>
            <if test="startMacAddress != null and startMacAddress != ''">
                start_mac_address = #{startMacAddress},
            </if>
            <if test="endMacAddress != null and endMacAddress != ''">
                end_mac_address = #{endMacAddress},
            </if>
            <if test="currentStock != null">
                current_stock = #{currentStock},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from bill.livestock where id = #{id}
    </delete>

</mapper>