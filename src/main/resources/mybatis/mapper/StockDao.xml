<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foxconn.sw.macaddress.dao.StockDao">

    <resultMap type="com.foxconn.sw.macaddress.entity.Stock" id="StockMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="startMacAddress" column="start_mac_address" jdbcType="VARCHAR"/>
        <result property="endMacAddress" column="end_mac_address" jdbcType="VARCHAR"/>
        <result property="stock" column="stock" jdbcType="INTEGER"/>
        <result property="createDate" column="create_date" jdbcType="TIMESTAMP"/>
        <result property="updateDate" column="update_date" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="StockMap">
        select
          id, start_mac_address, end_mac_address, stock, create_date, update_date, status
        from bill.stock
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="StockMap">
        select
          id, start_mac_address, end_mac_address, stock, create_date, update_date, status
        from bill.stock
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="StockMap">
        select
        id, start_mac_address, end_mac_address, stock, create_date, update_date, status
        from bill.stock
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="startMacAddress != null and startMacAddress != ''">
                and start_mac_address = #{startMacAddress}
            </if>
            <if test="endMacAddress != null and endMacAddress != ''">
                and end_mac_address = #{endMacAddress}
            </if>
            <if test="stock != null">
                and stock = #{stock}
            </if>
            <if test="createDate != null">
                and create_date = #{createDate}
            </if>
            <if test="updateDate != null">
                and update_date = #{updateDate}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into bill.stock(start_mac_address, end_mac_address, stock, create_date, update_date, status)
        values (#{startMacAddress}, #{endMacAddress}, #{stock}, #{createDate}, #{updateDate}, #{status})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bill.stock(start_mac_address, end_mac_address, stock, create_date, update_date, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.startMacAddress}, #{entity.endMacAddress}, #{entity.stock}, #{entity.createDate},
            #{entity.updateDate}, #{entity.status})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bill.stock(start_mac_address, end_mac_address, stock, create_date, update_date, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.startMacAddress}, #{entity.endMacAddress}, #{entity.stock}, #{entity.createDate},
            #{entity.updateDate}, #{entity.status})
        </foreach>
        on duplicate key update
        start_mac_address = values(start_mac_address) , end_mac_address = values(end_mac_address) , stock =
        values(stock) , create_date = values(create_date) , update_date = values(update_date) , status = values(status)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update bill.stock
        <set>
            <if test="startMacAddress != null and startMacAddress != ''">
                start_mac_address = #{startMacAddress},
            </if>
            <if test="endMacAddress != null and endMacAddress != ''">
                end_mac_address = #{endMacAddress},
            </if>
            <if test="stock != null">
                stock = #{stock},
            </if>
            <if test="createDate != null">
                create_date = #{createDate},
            </if>
            <if test="updateDate != null">
                update_date = #{updateDate},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from bill.stock where id = #{id}
    </delete>

</mapper>